# Сервис управления рассылками (Mailing Service)

Веб-приложение на Django для создания и управления email-рассылками. Сервис позволяет пользователям создавать рассылки, управлять получателями, отслеживать статистику отправок и многое другое.

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Установка с Poetry](#-установка-с-poetry)
- [Настройка](#-настройка)
- [Использование](#-использование)
- [Команды для управления](#-команды-для-управления)


## ✨ Возможности

### Для пользователей:
- ✅ Регистрация и аутентификация через email
- ✅ Подтверждение email при регистрации
- ✅ Восстановление пароля
- ✅ Управление профилем (аватар, телефон, страна)

### Для рассылок:
- ✅ Создание, редактирование и удаление сообщений
- ✅ Управление получателями (клиентами)
- ✅ Создание рассылок с привязкой к сообщениям и получателям
- ✅ Автоматическое вычисление статуса рассылки (Создана/Запущена/Завершена)
- ✅ Ручной и автоматический запуск рассылок
- ✅ Отслеживание попыток отправки

### Для менеджеров:
- ✅ Просмотр всех рассылок и клиентов
- ✅ Блокировка пользователей
- ✅ Отключение рассылок
- ✅ Просмотр статистики

### Дополнительно:
- ✅ Кеширование данных (Redis)
- ✅ Адаптивный дизайн (Bootstrap 5)
- ✅ Кастомные команды для управления
- ✅ Управление зависимостями через Poetry

## 🛠 Технологии

- **Backend:** Django 6.0, Python 3.13
- **База данных:** PostgreSQL
- **Кеширование:** Redis
- **Frontend:** Bootstrap 5, HTML5, CSS3
- **Аутентификация:** JWT + Session
- **Управление зависимостями:** Poetry
- **Дополнительно:** django-redis, python-dotenv, Pillow

## 📦 Установка с Poetry

### Предварительные требования

- Python 3.13 или выше
- PostgreSQL 14 или выше
- Redis 6 или выше
- Git
- Poetry (установка: `pip install poetry`)

### Пошаговая установка

1. **Клонируйте репозиторий:**
   ```conslole
   git clone https://github.com/ZloyKINDER/SendingMessages.git
   cd SendingMessages
   ```
   
2. **Установите зависимости через Poetry:**
   ```console
   # Активация виртуального окружения Poetry
   poetry shell
   
   # Или просто установите зависимости
   poetry install
   ```
    
3. **Создайте базу данных PostgreSQL:**
   ```sql
   CREATE DATABASE mailing_db;
   CREATE USER postgres WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE mailing_db TO postgres;
   ```
4. **Настройте переменные окружения:**
Создайте файл .env в корне проекта:
   ```env
   # Django Settings
   SECRET_KEY=
   DEBUG=
   
   # Database Settings
   NAME=
   USER=
   PASSWORD=
   HOST=
   PORT=
   
   # Email Settings (для Yandex)
   EMAIL_HOST =
   EMAIL_PORT =
   EMAIL_USE_TLS =
   EMAIL_USE_SSL =
   EMAIL_HOST_USER =
   EMAIL_HOST_PASSWORD =
   DEFAULT_FROM_EMAIL =
   ```
5. **Примените миграции:**
   ```console
   poetry run python manage.py migrate
   ```
6. **Создайте суперпользователя:**
   ```console
   poetry run python manage.py createsuperuser
   ```
7. **Создайте группы и права:**
   ```console
   poetry run python manage.py create_groups
   ```
8. **Запустите сервер разработки:**
   ```console
   poetry run python manage.py runserver
   ```
9. **Откройте в браузере: http://127.0.0.1:8000/**

## ⚙️ Настройка

### email (Yandex)

1. **Включите двухфакторную аутентификацию в аккаунте Yandex**
2. **Создайте пароль приложения в настройках безопасности**
3. **Используйте этот пароль в .env файле**

### Redis

### Windows:
- Скачайте Redis с https://github.com/microsoftarchive/redis/releases
- Установите и запустите службу Redis

## 🚀 Использование

### Основные функции

1. **Регистрация:**
   - Перейдите на /users/register/
   - Заполните форму регистрации
   - Подтвердите email по ссылке из письма
2. **Создание сообщения:**
   - Войдите в аккаунт
   - Перейдите в раздел "Сообщения"
   - Нажмите "Создать сообщение"
   - Заполните тему и текст
3. **Добавление получателей:**
   - Перейдите в раздел "Получатели"
   - Нажмите "Добавить получателя"
   - Введите email, ФИО и комментарий
4. **Создание рассылки:**
   - Перейдите в раздел "Рассылки"
   - Нажмите "Создать рассылку"
   - Выберите сообщение и получателей
   - Укажите даты начала и окончания
5. **Запуск рассылки:**
   - В разделе "Рассылки" выберите нужную
   - Нажмите "Запустить сейчас"
   - Отслеживайте статус в разделе "Попытки"

## 🔧 Команды для управления

### Отправка рассылок
   ```console
   # Отправка всех активных рассылок
   poetry run python manage.py send_mailings
   
   # Отправка конкретной рассылки
   poetry run python manage.py send_mailings --mailing-id 1
   
   # Отправка рассылок конкретного пользователя
   poetry run python manage.py send_mailings --user-email user@example.com
   
   # Принудительная отправка
   poetry run python manage.py send_mailings --force
   
   # Пробный запуск (без реальной отправки)
   poetry run python manage.py send_mailings --dry-run
   ```

### Отправка рассылок
   ```console
   # Отправка всех активных рассылок
   poetry run python manage.py send_mailings
   
   # Отправка конкретной рассылки
   poetry run python manage.py send_mailings --mailing-id 1
   
   # Отправка рассылок конкретного пользователя
   poetry run python manage.py send_mailings --user-email user@example.com
   
   # Принудительная отправка
   poetry run python manage.py send_mailings --force
   
   # Пробный запуск (без реальной отправки)
   poetry run python manage.py send_mailings --dry-run
   ```

### Статистика
   ```console
   # Статистика за последние 7 дней
   poetry run python manage.py mailing_stats
   
   # Статистика за 30 дней
   poetry run python manage.py mailing_stats --days 30
   
   # Статистика конкретного пользователя
   poetry run python manage.py mailing_stats --user-email user@example.com
   ```

### Очистка данных
   ```console
   # Очистка попыток старше 30 дней
   poetry run python manage.py cleanup_attempts
   
   # Очистка попыток старше 60 дней
   poetry run python manage.py cleanup_attempts --days 60
   
   # Пробный запуск очистки
   poetry run python manage.py cleanup_attempts --dry-run
   ```

### Управление группами
   ```console
   # Создание групп и прав
   poetry run python manage.py create_groups
   
   # Создание тестового менеджера
   poetry run python manage.py create_groups --create-manager
   ```
